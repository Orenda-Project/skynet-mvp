# FastAPI and web server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Database - Supabase PostgreSQL
# We're using PostgreSQL directly via SQLAlchemy, no need for Supabase Python client yet
sqlalchemy>=2.0.36
psycopg2-binary>=2.9.10  # PostgreSQL driver (binary version for easy deployment)
alembic>=1.14.0
# supabase>=2.10.0  # Disabled: Python 3.14 compatibility issues. Can add later if needed for Auth/Storage

# Pydantic settings and validation
pydantic>=2.10.0
pydantic-settings>=2.6.0
email-validator>=2.2.0

# LLM Integration - OpenAI
openai>=1.63.0

# Transcription - Soniox (will add later when API available)
# For now, we'll use OpenAI Whisper as alternative
# soniox==<version>

# HTTP clients
httpx>=0.28.1
aiohttp>=3.11.0

# Email
aiosmtplib>=3.0.2

# Templates
jinja2>=3.1.5

# Logging and observability
structlog>=24.4.0
python-json-logger>=3.2.1

# Environment variables
python-dotenv>=1.0.1

# Testing
pytest>=8.3.0
pytest-asyncio>=0.25.1
pytest-cov>=6.0.0

# Code quality
black>=24.8.0
flake8>=7.1.1
mypy>=1.14.0
pre-commit>=4.0.1

# Type stubs
types-aiofiles>=24.1.0
types-requests>=2.32.0

# Utilities
python-dateutil>=2.9.0
